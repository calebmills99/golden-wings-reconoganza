# Golden Wings Documentary - File Rename Execution Script
# Generated by Step 1.4 on 2025-09-27 10:03:12
# Source mappings: D:\golden-wings-reconoganza\rename_mappings.json

[CmdletBinding()]
param(
    [switch]$WhatIf,
    [switch]$Force,
    [switch]$NoBackup,
    [string]$LogFile
)

Write-Host "🎬 Golden Wings File Rename Execution" -ForegroundColor Yellow
Write-Host "====================================" -ForegroundColor Yellow
Write-Host ""

# Initialize logging
if ([string]::IsNullOrWhiteSpace($LogFile)) {
    $timestamp = Get-Date -Format "yyyyMMdd_HHmmss"
    $LogFile = "rename_execution_log_$timestamp.txt"
}

function Write-Log {
    param([string]$Message, [string]$Level = "INFO")
    $timestamp = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
    $logEntry = "[$timestamp] [$Level] $Message"
    Add-Content -Path $LogFile -Value $logEntry -Encoding UTF8
    
    switch ($Level) {
        "ERROR" { Write-Host $Message -ForegroundColor Red }
        "WARNING" { Write-Host $Message -ForegroundColor Yellow }
        "SUCCESS" { Write-Host $Message -ForegroundColor Green }
        default { Write-Host $Message -ForegroundColor White }
    }
}

# Initialize counters
$summary = @{
    TotalFiles = 46
    Renamed = 0
    Skipped = 0
    Errors = 0
    BackedUp = 0
}

Write-Log "Starting rename operation for $($summary.TotalFiles) files"
Write-Log "Log file: $LogFile"
Write-Log "What-If mode: $($WhatIf.IsPresent)"
Write-Log "Force mode: $($Force.IsPresent)"
Write-Log "Backup disabled: $($NoBackup.IsPresent)"
Write-Host ""

# Create backup directory if needed
$backupDir = $null
if (-not $NoBackup -and -not $WhatIf) {
    $backupDir = "backup_20250927_100312"
    try {
        New-Item -Path $backupDir -ItemType Directory -Force | Out-Null
        Write-Log "Created backup directory: $backupDir" "SUCCESS"
    } catch {
        Write-Log "Failed to create backup directory: $($_.Exception.Message)" "ERROR"
        if (-not $Force) {
            Write-Log "Aborting due to backup directory creation failure. Use -Force to continue without backup." "ERROR"
            exit 1
        }
        $backupDir = $null
    }
}

# Process each rename mapping
# Rename: filmfreeway-transactions-1.csv → GW_Contacts_FilmFreeway_2024-10-29.csv
try {
    $oldPath = 'D:\gdrive\Organized\documents\financial\filmfreeway-transactions-1.csv'
    $newPath = 'D:\gdrive\Organized\documents\financial\GW_Contacts_FilmFreeway_2024-10-29.csv'
    $oldFileName = 'filmfreeway-transactions-1.csv'
    $newFileName = 'GW_Contacts_FilmFreeway_2024-10-29.csv'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: contacts-1-1.csv → GW_Contacts_General_2025-02-03.csv
try {
    $oldPath = 'D:\gdrive\Organized\documents\financial\contacts-1-1.csv'
    $newPath = 'D:\gdrive\Organized\documents\financial\GW_Contacts_General_2025-02-03.csv'
    $oldFileName = 'contacts-1-1.csv'
    $newFileName = 'GW_Contacts_General_2025-02-03.csv'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: contacts.csv → GW_Contacts_General_2025-02-03.csv
try {
    $oldPath = 'D:\gdrive\burningman\contacts.csv'
    $newPath = 'D:\gdrive\burningman\GW_Contacts_General_2025-02-03.csv'
    $oldFileName = 'contacts.csv'
    $newFileName = 'GW_Contacts_General_2025-02-03.csv'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: CALEB.CSV → GW_Contacts_Caleb_Personal_2024-12-04.csv
try {
    $oldPath = 'D:\gdrive\burningman\root_misc\Program Files\HWiNFO64\CALEB.CSV'
    $newPath = 'D:\gdrive\burningman\root_misc\Program Files\HWiNFO64\GW_Contacts_Caleb_Personal_2024-12-04.csv'
    $oldFileName = 'CALEB.CSV'
    $newFileName = 'GW_Contacts_Caleb_Personal_2024-12-04.csv'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: final_cleaned_contacts_for_squarespace-1.csv → GW_Contacts_Squarespace_2025-02-03.csv
try {
    $oldPath = 'D:\gdrive\Organized\documents\financial\final_cleaned_contacts_for_squarespace-1.csv'
    $newPath = 'D:\gdrive\Organized\documents\financial\GW_Contacts_Squarespace_2025-02-03.csv'
    $oldFileName = 'final_cleaned_contacts_for_squarespace-1.csv'
    $newFileName = 'GW_Contacts_Squarespace_2025-02-03.csv'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: cleaned_contacts.csv → GW_Contacts_General_2025-02-03_v2.csv
try {
    $oldPath = 'D:\gdrive\burningman\cleaned_contacts.csv'
    $newPath = 'D:\gdrive\burningman\GW_Contacts_General_2025-02-03_v2.csv'
    $oldFileName = 'cleaned_contacts.csv'
    $newFileName = 'GW_Contacts_General_2025-02-03_v2.csv'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: Golden Wings Transcript.txt → Transcript_Multiple_Golden_Wings_Story_Main.txt
try {
    $oldPath = 'C:\Users\nobby\Desktop\Golden Wings Transcript.txt'
    $newPath = 'C:\Users\nobby\Desktop\Transcript_Multiple_Golden_Wings_Story_Main.txt'
    $oldFileName = 'Golden Wings Transcript.txt'
    $newFileName = 'Transcript_Multiple_Golden_Wings_Story_Main.txt'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: Golden WIngs Transcript-Sept25.txt → Transcript_Multiple_Golden_Wings_Story_Main.txt
try {
    $oldPath = 'D:\Film Projects\Golden Wings\Golden WIngs Transcript-Sept25.txt'
    $newPath = 'D:\Film Projects\Golden Wings\Transcript_Multiple_Golden_Wings_Story_Main.txt'
    $oldFileName = 'Golden WIngs Transcript-Sept25.txt'
    $newFileName = 'Transcript_Multiple_Golden_Wings_Story_Main.txt'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: MisterAmericanAirlines.txt → Transcript_Jock_Bethune_American_Airlines_Career_Interview.txt
try {
    $oldPath = 'G:\dev\WhisperPS\MisterAmericanAirlines.txt'
    $newPath = 'G:\dev\WhisperPS\Transcript_Jock_Bethune_American_Airlines_Career_Interview.txt'
    $oldFileName = 'MisterAmericanAirlines.txt'
    $newFileName = 'Transcript_Jock_Bethune_American_Airlines_Career_Interview.txt'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: caleb_voice.txt → Transcript_Caleb_Stewart_Director_Notes_Voice.txt
try {
    $oldPath = 'C:\Users\nobby\Desktop\caleb_voice.txt'
    $newPath = 'C:\Users\nobby\Desktop\Transcript_Caleb_Stewart_Director_Notes_Voice.txt'
    $oldFileName = 'caleb_voice.txt'
    $newFileName = 'Transcript_Caleb_Stewart_Director_Notes_Voice.txt'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: kick-2nd-half_otter_ai-1.txt → Transcript_Unknown_Interview_Transcript.txt
try {
    $oldPath = 'D:\gdrive\Organized\documents\personal\kick-2nd-half_otter_ai-1.txt'
    $newPath = 'D:\gdrive\Organized\documents\personal\Transcript_Unknown_Interview_Transcript.txt'
    $oldFileName = 'kick-2nd-half_otter_ai-1.txt'
    $newFileName = 'Transcript_Unknown_Interview_Transcript.txt'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: Caleb_All_Around_Film_Festival_Interview.txt → Transcript_Unknown_Interview_Transcript.txt
try {
    $oldPath = 'G:\dev\WhisperPS\Caleb_All_Around_Film_Festival_Interview.txt'
    $newPath = 'G:\dev\WhisperPS\Transcript_Unknown_Interview_Transcript.txt'
    $oldFileName = 'Caleb_All_Around_Film_Festival_Interview.txt'
    $newFileName = 'Transcript_Unknown_Interview_Transcript.txt'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: folder_structure.txt → GW_System_Directory_Structure_2024-10-26.txt
try {
    $oldPath = 'D:\gdrive\burningman\root_misc\Program Files\NVIDIA Corporation\ChatRTX\folder_structure.txt'
    $newPath = 'D:\gdrive\burningman\root_misc\Program Files\NVIDIA Corporation\ChatRTX\GW_System_Directory_Structure_2024-10-26.txt'
    $oldFileName = 'folder_structure.txt'
    $newFileName = 'GW_System_Directory_Structure_2024-10-26.txt'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: CHANGES.md → GW_System_Change_Log_2024-12-02.md
try {
    $oldPath = 'D:\gdrive\burningman\root_misc\cmake-3.31.1\openssl\CHANGES.md'
    $newPath = 'D:\gdrive\burningman\root_misc\cmake-3.31.1\openssl\GW_System_Change_Log_2024-12-02.md'
    $oldFileName = 'CHANGES.md'
    $newFileName = 'GW_System_Change_Log_2024-12-02.md'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: Files.txt → GW_System_File_List_2024-09-17.txt
try {
    $oldPath = 'D:\gdrive\burningman\run\Files.txt'
    $newPath = 'D:\gdrive\burningman\run\GW_System_File_List_2024-09-17.txt'
    $oldFileName = 'Files.txt'
    $newFileName = 'GW_System_File_List_2024-09-17.txt'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: profile_update_history.html → GW_Web_Facebook_Profile_History.html
try {
    $oldPath = 'D:\gdrive\burningman\root_misc\facebook-calebstewar\personal_information\profile_information\profile_update_history.html'
    $newPath = 'D:\gdrive\burningman\root_misc\facebook-calebstewar\personal_information\profile_information\GW_Web_Facebook_Profile_History.html'
    $oldFileName = 'profile_update_history.html'
    $newFileName = 'GW_Web_Facebook_Profile_History.html'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: Facebook.txt → GW_Web_Facebook_Export.html
try {
    $oldPath = 'C:\Users\nobby\Documents\Projects\FACEBOOK\Facebook.txt'
    $newPath = 'C:\Users\nobby\Documents\Projects\FACEBOOK\GW_Web_Facebook_Export.html'
    $oldFileName = 'Facebook.txt'
    $newFileName = 'GW_Web_Facebook_Export.html'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: fb_video_page.html → GW_Web_Facebook_Export.html
try {
    $oldPath = 'C:\Users\nobby\Documents\fb_video_page.html'
    $newPath = 'C:\Users\nobby\Documents\GW_Web_Facebook_Export.html'
    $oldFileName = 'fb_video_page.html'
    $newFileName = 'GW_Web_Facebook_Export.html'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: login.html → GW_Web_Web_Login_Page.html
try {
    $oldPath = 'D:\gdrive\burningman\Doxxer-Toolkit\.pages\en_pages\google_new\login.html'
    $newPath = 'D:\gdrive\burningman\Doxxer-Toolkit\.pages\en_pages\google_new\GW_Web_Web_Login_Page.html'
    $oldFileName = 'login.html'
    $newFileName = 'GW_Web_Web_Login_Page.html'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: login_1.html → GW_Web_Web_Login_Page.html
try {
    $oldPath = 'D:\gdrive\burningman\Doxxer-Toolkit\.pages\Google\login_1.html'
    $newPath = 'D:\gdrive\burningman\Doxxer-Toolkit\.pages\Google\GW_Web_Web_Login_Page.html'
    $oldFileName = 'login_1.html'
    $newFileName = 'GW_Web_Web_Login_Page.html'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: ffmpeg-all.html → GW_Web_Web_Page.html
try {
    $oldPath = 'D:\gdrive\burningman\root_misc\ffmpeg\doc\ffmpeg-all.html'
    $newPath = 'D:\gdrive\burningman\root_misc\ffmpeg\doc\GW_Web_Web_Page.html'
    $oldFileName = 'ffmpeg-all.html'
    $newFileName = 'GW_Web_Web_Page.html'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: veterans_search_response.html → GW_Web_Veterans_Site_Search_Results.html
try {
    $oldPath = 'D:\gdrive\burningman\veterans_search_response.html'
    $newPath = 'D:\gdrive\burningman\GW_Web_Veterans_Site_Search_Results.html'
    $oldFileName = 'veterans_search_response.html'
    $newFileName = 'GW_Web_Veterans_Site_Search_Results.html'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: ffmpeg-formats.html → GW_Web_Web_Page_v2.html
try {
    $oldPath = 'D:\gdrive\burningman\root_misc\ffmpeg\doc\ffmpeg-formats.html'
    $newPath = 'D:\gdrive\burningman\root_misc\ffmpeg\doc\GW_Web_Web_Page_v2.html'
    $oldFileName = 'ffmpeg-formats.html'
    $newFileName = 'GW_Web_Web_Page_v2.html'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: ffplay-all.html → GW_Web_Web_Page_v3.html
try {
    $oldPath = 'D:\gdrive\burningman\root_misc\ffmpeg\doc\ffplay-all.html'
    $newPath = 'D:\gdrive\burningman\root_misc\ffmpeg\doc\GW_Web_Web_Page_v3.html'
    $oldFileName = 'ffplay-all.html'
    $newFileName = 'GW_Web_Web_Page_v3.html'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: Golden_Wings_30sec.txt → GW_Document_General_Document.txt
try {
    $oldPath = 'G:\dev\WhisperPS\Golden_Wings_30sec.txt'
    $newPath = 'G:\dev\WhisperPS\GW_Document_General_Document.txt'
    $oldFileName = 'Golden_Wings_30sec.txt'
    $newFileName = 'GW_Document_General_Document.txt'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: golden_wings_press_release.md → GW_Document_Marketing_Press_Release.md
try {
    $oldPath = 'D:\gdrive\burningman\run\golden_wings_press_release.md'
    $newPath = 'D:\gdrive\burningman\run\GW_Document_Marketing_Press_Release.md'
    $oldFileName = 'golden_wings_press_release.md'
    $newFileName = 'GW_Document_Marketing_Press_Release.md'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: stewardess college history.txt → GW_Document_Historical_Background_Research.txt
try {
    $oldPath = 'D:\gdrive\burningman\run\stewardess college history.txt'
    $newPath = 'D:\gdrive\burningman\run\GW_Document_Historical_Background_Research.txt'
    $oldFileName = 'stewardess college history.txt'
    $newFileName = 'GW_Document_Historical_Background_Research.txt'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: Golden Wings History.txt → GW_Document_Historical_Background_Research.txt
try {
    $oldPath = 'C:\Users\nobby\Desktop\Golden Wings History.txt'
    $newPath = 'C:\Users\nobby\Desktop\GW_Document_Historical_Background_Research.txt'
    $oldFileName = 'Golden Wings History.txt'
    $newFileName = 'GW_Document_Historical_Background_Research.txt'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: project description.txt → GW_Document_Planning_Project_Overview.txt
try {
    $oldPath = 'D:\gdrive\burningman\run\project description.txt'
    $newPath = 'D:\gdrive\burningman\run\GW_Document_Planning_Project_Overview.txt'
    $oldFileName = 'project description.txt'
    $newFileName = 'GW_Document_Planning_Project_Overview.txt'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: Golden Wings History.txt → GW_Document_Historical_Background_Research.txt
try {
    $oldPath = 'C:\Users\nobby\Desktop\Desktop\Old_PC_Migration\Documents\Golden Wings History.txt'
    $newPath = 'C:\Users\nobby\Desktop\Desktop\Old_PC_Migration\Documents\GW_Document_Historical_Background_Research.txt'
    $oldFileName = 'Golden Wings History.txt'
    $newFileName = 'GW_Document_Historical_Background_Research.txt'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: Golden Wings History.txt → GW_Document_Historical_Background_Research.txt
try {
    $oldPath = 'C:\Users\nobby\Documents\Golden Wings History.txt'
    $newPath = 'C:\Users\nobby\Documents\GW_Document_Historical_Background_Research.txt'
    $oldFileName = 'Golden Wings History.txt'
    $newFileName = 'GW_Document_Historical_Background_Research.txt'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: links_site1.txt → GW_Document_General_Document.txt
try {
    $oldPath = 'D:\gdrive\burningman\run\links_site1.txt'
    $newPath = 'D:\gdrive\burningman\run\GW_Document_General_Document.txt'
    $oldFileName = 'links_site1.txt'
    $newFileName = 'GW_Document_General_Document.txt'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: links_site1.txt → GW_Document_General_Document.txt
try {
    $oldPath = 'D:\gdrive\burningman\links_site1.txt'
    $newPath = 'D:\gdrive\burningman\GW_Document_General_Document.txt'
    $oldFileName = 'links_site1.txt'
    $newFileName = 'GW_Document_General_Document.txt'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: NEWS.txt → GW_Unknown_19_2024-12-27.txt
try {
    $oldPath = 'D:\gdrive\burningman\root_misc\Program Files\MKVToolNix\doc\NEWS.txt'
    $newPath = 'D:\gdrive\burningman\root_misc\Program Files\MKVToolNix\doc\GW_Unknown_19_2024-12-27.txt'
    $oldFileName = 'NEWS.txt'
    $newFileName = 'GW_Unknown_19_2024-12-27.txt'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: duplicates.txt → GW_Report_Duplicate_Analysis_2024-12-13.txt
try {
    $oldPath = 'D:\gdrive\burningman\duplicates.txt'
    $newPath = 'D:\gdrive\burningman\GW_Report_Duplicate_Analysis_2024-12-13.txt'
    $oldFileName = 'duplicates.txt'
    $newFileName = 'GW_Report_Duplicate_Analysis_2024-12-13.txt'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: deep_duplicate_report_20250711_195958.txt → GW_Report_Duplicate_Analysis_2025-07-11.txt
try {
    $oldPath = 'D:\gdrive\deep_duplicate_report_20250711_195958.txt'
    $newPath = 'D:\gdrive\GW_Report_Duplicate_Analysis_2025-07-11.txt'
    $oldFileName = 'deep_duplicate_report_20250711_195958.txt'
    $newFileName = 'GW_Report_Duplicate_Analysis_2025-07-11.txt'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: stats_20241213_134916.txt → GW_Report_Statistics_2024-12-13.txt
try {
    $oldPath = 'D:\gdrive\burningman\output\stats_20241213_134916.txt'
    $newPath = 'D:\gdrive\burningman\output\GW_Report_Statistics_2024-12-13.txt'
    $oldFileName = 'stats_20241213_134916.txt'
    $newFileName = 'GW_Report_Statistics_2024-12-13.txt'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: duplicate_report.txt → GW_Report_Duplicate_Analysis_2025-07-11.txt
try {
    $oldPath = 'D:\gdrive\Organized\videos\other\duplicate_report.txt'
    $newPath = 'D:\gdrive\Organized\videos\other\GW_Report_Duplicate_Analysis_2025-07-11.txt'
    $oldFileName = 'duplicate_report.txt'
    $newFileName = 'GW_Report_Duplicate_Analysis_2025-07-11.txt'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: quick_duplicate_report.txt → GW_Report_Duplicate_Analysis_2025-07-11_v2.txt
try {
    $oldPath = 'D:\gdrive\Organized\videos\other\quick_duplicate_report.txt'
    $newPath = 'D:\gdrive\Organized\videos\other\GW_Report_Duplicate_Analysis_2025-07-11_v2.txt'
    $oldFileName = 'quick_duplicate_report.txt'
    $newFileName = 'GW_Report_Duplicate_Analysis_2025-07-11_v2.txt'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: cleanup_log.txt → GW_Report_Cleanup_Log_2025-07-11.txt
try {
    $oldPath = 'D:\gdrive\Organized\videos\other\cleanup_log.txt'
    $newPath = 'D:\gdrive\Organized\videos\other\GW_Report_Cleanup_Log_2025-07-11.txt'
    $oldFileName = 'cleanup_log.txt'
    $newFileName = 'GW_Report_Cleanup_Log_2025-07-11.txt'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: FEVER_ENHANCED_STRATEGY.md → GW_Strategy_Festival_Strategy_Enhanced.md
try {
    $oldPath = 'C:\Users\nobby\Desktop\Desktop\Old_PC_Migration\Documents\Projects\chatui_Zero2x\FEVER_ENHANCED_STRATEGY.md'
    $newPath = 'C:\Users\nobby\Desktop\Desktop\Old_PC_Migration\Documents\Projects\chatui_Zero2x\GW_Strategy_Festival_Strategy_Enhanced.md'
    $oldFileName = 'FEVER_ENHANCED_STRATEGY.md'
    $newFileName = 'GW_Strategy_Festival_Strategy_Enhanced.md'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: FEVER_ENHANCED_STRATEGY.md → GW_Strategy_Festival_Strategy_Enhanced.md
try {
    $oldPath = 'C:\Users\nobby\Documents\Projects\chatui_Zero2x\FEVER_ENHANCED_STRATEGY.md'
    $newPath = 'C:\Users\nobby\Documents\Projects\chatui_Zero2x\GW_Strategy_Festival_Strategy_Enhanced.md'
    $oldFileName = 'FEVER_ENHANCED_STRATEGY.md'
    $newFileName = 'GW_Strategy_Festival_Strategy_Enhanced.md'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: FEVER_ULTIMATE_STRATEGY.md → GW_Strategy_Festival_Strategy_Ultimate.md
try {
    $oldPath = 'C:\Users\nobby\Desktop\Desktop\Old_PC_Migration\Documents\Projects\chatui_Zero2x\FEVER_ULTIMATE_STRATEGY.md'
    $newPath = 'C:\Users\nobby\Desktop\Desktop\Old_PC_Migration\Documents\Projects\chatui_Zero2x\GW_Strategy_Festival_Strategy_Ultimate.md'
    $oldFileName = 'FEVER_ULTIMATE_STRATEGY.md'
    $newFileName = 'GW_Strategy_Festival_Strategy_Ultimate.md'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: FEVER_ULTIMATE_STRATEGY.md → GW_Strategy_Festival_Strategy_Ultimate.md
try {
    $oldPath = 'C:\Users\nobby\Documents\Projects\chatui_Zero2x\FEVER_ULTIMATE_STRATEGY.md'
    $newPath = 'C:\Users\nobby\Documents\Projects\chatui_Zero2x\GW_Strategy_Festival_Strategy_Ultimate.md'
    $oldFileName = 'FEVER_ULTIMATE_STRATEGY.md'
    $newFileName = 'GW_Strategy_Festival_Strategy_Ultimate.md'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: FEVER_LA_STRATEGY_SUMMARY.md → GW_Strategy_LA_Festival_Strategy_Summary.md
try {
    $oldPath = 'C:\Users\nobby\Desktop\Desktop\Old_PC_Migration\Documents\Projects\chatui_Zero2x\FEVER_LA_STRATEGY_SUMMARY.md'
    $newPath = 'C:\Users\nobby\Desktop\Desktop\Old_PC_Migration\Documents\Projects\chatui_Zero2x\GW_Strategy_LA_Festival_Strategy_Summary.md'
    $oldFileName = 'FEVER_LA_STRATEGY_SUMMARY.md'
    $newFileName = 'GW_Strategy_LA_Festival_Strategy_Summary.md'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
# Rename: FEVER_LA_STRATEGY_SUMMARY.md → GW_Strategy_LA_Festival_Strategy_Summary.md
try {
    $oldPath = 'C:\Users\nobby\Documents\Projects\chatui_Zero2x\FEVER_LA_STRATEGY_SUMMARY.md'
    $newPath = 'C:\Users\nobby\Documents\Projects\chatui_Zero2x\GW_Strategy_LA_Festival_Strategy_Summary.md'
    $oldFileName = 'FEVER_LA_STRATEGY_SUMMARY.md'
    $newFileName = 'GW_Strategy_LA_Festival_Strategy_Summary.md'
    
    Write-Log "Processing: $oldFileName → $newFileName"
    
    # Verify source file still exists
    if (-not (Test-Path -LiteralPath $oldPath)) {
        Write-Log "Source file not found: $oldPath" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Check if destination already exists
    if ((Test-Path -LiteralPath $newPath) -and -not $Force) {
        Write-Log "Destination already exists: $newPath (use -Force to overwrite)" "WARNING"
        $summary.Skipped++
        continue
    }
    
    # Create destination directory if needed
    $destDir = Split-Path -Path $newPath -Parent
    if (-not (Test-Path -Path $destDir)) {
        if (-not $WhatIf) {
            New-Item -Path $destDir -ItemType Directory -Force | Out-Null
            Write-Log "Created directory: $destDir"
        } else {
            Write-Log "Would create directory: $destDir"
        }
    }
    
    # Create backup if enabled
    if ($backupDir -and -not $WhatIf) {
        try {
            $backupPath = Join-Path -Path $backupDir -ChildPath $oldFileName
            Copy-Item -LiteralPath $oldPath -Destination $backupPath -Force
            Write-Log "Backed up: $oldFileName"
            $summary.BackedUp++
        } catch {
            Write-Log "Backup failed for $oldFileName: $($_.Exception.Message)" "WARNING"
        }
    }
    
    # Perform the rename
    if ($WhatIf) {
        Write-Log "Would rename: $oldPath → $newPath"
    } else {
        Move-Item -LiteralPath $oldPath -Destination $newPath -Force
        Write-Log "Renamed: $oldFileName → $newFileName" "SUCCESS"
    }
    
    $summary.Renamed++
    
} catch {
    Write-Log "Error renaming $oldFileName: $($_.Exception.Message)" "ERROR"
    $summary.Errors++
}
Write-Host ""
Write-Host "====================================" -ForegroundColor Yellow
Write-Host "📊 RENAME OPERATION SUMMARY" -ForegroundColor Yellow
Write-Host "====================================" -ForegroundColor Yellow
Write-Log "Total files: $($summary.TotalFiles)"
Write-Log "Successfully renamed: $($summary.Renamed)" "SUCCESS"
Write-Log "Skipped: $($summary.Skipped)" "WARNING"
Write-Log "Errors: $($summary.Errors)" "ERROR"
if ($backupDir) {
    Write-Log "Files backed up: $($summary.BackedUp)" "SUCCESS"
    Write-Log "Backup location: $backupDir"
}
Write-Log "Log file: $LogFile"

if ($summary.Errors -gt 0) {
    Write-Log "Rename operation completed with errors. Check log for details." "WARNING"
    exit 1
} else {
    Write-Log "Rename operation completed successfully!" "SUCCESS"
    exit 0
}
